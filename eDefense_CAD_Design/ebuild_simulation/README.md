# eDefense — eBuild Simulation

Simulation targets: RF link budget, encryption throughput, biometric recognition latency, sensor fusion.

```bash
cd eDefense_CAD_Design/{product}/simulation/
python3 power_budget_sim.py
```
