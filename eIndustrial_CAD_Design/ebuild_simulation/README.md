# eIndustrial — eBuild Simulation

Simulation targets: fieldbus timing (PROFIBUS/EtherCAT), power budget, sensor calibration curves, PLC ladder logic execution.

```bash
cd eIndustrial_CAD_Design/{product}/simulation/
python3 power_budget_sim.py
```
