# eTransport — eBuild Simulation

Simulation targets: CAN FD timing, ASIL-D safety monitor, NMEA 2000 message parsing, ATP safety logic.

```bash
cd eTransport_CAD_Design/{product}/simulation/
python3 power_budget_sim.py
```
